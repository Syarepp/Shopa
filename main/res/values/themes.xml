<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Shoppa" parent="Theme.Material3.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/shoppa_primary</item>
        <item name="colorPrimaryDark">@color/shoppa_primary_dark</item>
        <item name="colorAccent">@color/shoppa_accent</item>

        <!-- Surface colors -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/shoppa_primary_dark</item>
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Remove the splash background reference -->
    </style>

    <style name="Theme.Shoppa" parent="Base.Theme.Shoppa" />

    <!-- Remove the splash screen theme or fix it -->
</resources>